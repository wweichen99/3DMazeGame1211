透视功能：保留了上一轮的青色透视墙体和红色终点导航线。

视口采集：每 250ms 记录一次数据，包含相机位置和相机朝向与“起点”、“终点”的夹角（角度制）。

Minimap热图：记录了用户在右上角 Minimap 上的鼠标悬停（hovers）和点击（clicks）次数，数据格式为网格坐标 "x,y"。

数据导出：点击右上角的 "Export Data (JSON)" 按钮即可下载所有采集到的数据。